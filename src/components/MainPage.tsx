import { FC } from "react";

const MainPage: FC = () => {
  return (
    <div>
      <h1>My First React App</h1>
      <p>{`Welcome :)`}</p>
      <button></button>
    </div>
  );
};

export default MainPage;
